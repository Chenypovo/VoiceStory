services:
  voice-story:
    build: .
    ports:
      - "7860:7860"
    environment:
      - BAILIAN_API_KEY=${BAILIAN_API_KEY}
    volumes:
      - voice-profiles:/app/voice/profiles
      - outputs:/app/outputs
    restart: unless-stopped

volumes:
  voice-profiles:
  outputs:
